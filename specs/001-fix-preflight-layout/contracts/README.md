# Contracts Summary: Status Form Layout Corrections

No new HTTP endpoints or schema changes are required. Existing `/match` and `/prop` payloads continue to drive the UI through MatchCoordinator/RelaySnapshot; this document notes that the UI remains a read-only consumer of those contracts.
